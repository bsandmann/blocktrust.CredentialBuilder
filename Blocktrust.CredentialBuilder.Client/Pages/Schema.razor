@page "/Schema"
@using Blocktrust.CredentialBuilder.Client.Services
@using Blocktrust.CredentialBuilder.Client.Models
@using Blocktrust.CredentialBuilder.Client.Components
@inject IAgentService AgentService
@inject ISchemaService SchemaService
@inject ISnackbar Snackbar

<PageTitle>Blocktrust.CredentialBuilder</PageTitle>
<MudStack>

<MudText>Not yet supported</MudText>
    @*<MudButton OnClick="GetSchemas">Get Schemas</MudButton>*@
</MudStack>

@code {

    protected override async Task OnInitializedAsync()
    {
        await AgentService.GetAgents();
    }


    private void GetSchemas()
    {
    //    Agent? someAgent = null;
    //    if (AgentService is not null && AgentService.AgentsInMemory is not null)
    //    {
    //        someAgent = AgentService.AgentsInMemory.List.Skip(0).FirstOrDefault();
    //    }
    //    if (someAgent is null)
    //    {
    ////TODO 
    //    }
    //    var r = SchemaService.GetListSchemas(someAgent!);
    }

}